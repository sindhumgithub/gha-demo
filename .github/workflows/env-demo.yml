name: Env demo
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  env-demo:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest

    steps:
      - name: Connect to Env
        env:
          APP_ENV: "production"
        run: echo "App is connecting to $APP_ENV"
