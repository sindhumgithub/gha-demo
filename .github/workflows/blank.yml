name: Hello World
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello Step
        run: echo "This is my github line one"
  
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Phase
        run: echo "Running test cases"

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the App
        run: echo "Deploying the app"
